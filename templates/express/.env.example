# Server Configuration
PORT=3000
NODE_ENV=development

# Database
MONGO_URI=

# Optional: when rate limiting is enabled (see config/features.ts)
# RATE_LIMIT_MAX=100

# Optional: when auth is enabled (header or cookie). Use a strong secret in production.
# JWT_SECRET=your-jwt-secret